//- this block shows the premium and not so premium food and drink listings inside common food listings

- var categoryId = "2I8IujX52EAy8uqauEkcm8"
- var thePages = contentful.pages

each page in thePages
	if page.belongsToCategory(categoryId)
		+c-listing--premium (page.getGalleryImages(language),'#',page.getLanguageUrl(language),page.price)
			p= page.getDescription(language)

.flexorama--row
	//- load the c-listing--secondary mixin
	each page in thePages
		if page.belongsToCategory(categoryId)
			+c-listing--secondary (page.getGalleryImages(language),'#',page.getLanguageUrl(language))
